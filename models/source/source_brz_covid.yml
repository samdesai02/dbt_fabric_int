version: 2

sources:
  - name: covid_data
    database: dw_dbt_fabric_covid
    schema: dbo
    tables:
      - name: raw_covid_data
        columns:
        - name: id
          data_type: varchar
          description: "Unique identifier for each event."
          tests:
            - not_null
            - unique
        - name: confirmed
          data_type: float
          description: "Unique identifier for each event."            
